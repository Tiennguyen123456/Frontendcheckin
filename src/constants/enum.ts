export enum DeviceType {
  Mobile = "mobile",
  Tablet = "tablet",
  Desktop = "desktop",
}

export enum Languages {
  English = "en",
  VietNamese = "vi",
}

export enum EventStatus {
  New = "NEW",
  Active = "ACTIVE",
  Inactive = "INACTIVE",
  Done = "DONE",
  Cancel = "CANCEL",
}
